#include <iostream>

using namespace std;

struct Node {
    string name;
    string drinkOrder;
    Node* next;
};

const string names[] = {"Alicia", "Frank"};
const string drinkOrders[] = {"Cappuccino", "Mocha", "Americano"};

int main() {
    int count = 0;
    while (count < 10) {
        
        count++;
    }

}